# Install torch/torchaudio FIRST from the CUDA index that matches your GPU, e.g.
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu130
# then:
#   pip install chatterbox-tts --no-deps
#   pip install -r requirements.txt
# chatterbox-tts pins torch==2.6.0, which has no Blackwell (sm_120) kernels; --no-deps sidesteps that.
numpy<2.0.0
librosa==0.11.0
s3tokenizer
transformers==5.2.0
diffusers==0.29.0
resemble-perth>=1.0.0
conformer==0.3.2
safetensors==0.5.3
spacy-pkuseg
pykakasi==2.3.0
pyloudnorm
omegaconf
huggingface_hub
fastapi
uvicorn[standard]
